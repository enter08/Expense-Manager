<!DOCTYPE HTML>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Expense manager</title>
  </head>
  <body>
  <div class="col-md-5 center-block">
    <div class="panel panel-default">
      <div class="panel-heading">Monthly budget management</div>
      <div class="panel-body">
        <p>Enter the amount of money you want to spend by your <%= link_to "active categories", categories_customize_path %>. If you don't want to spend money in some category, leave the field blank.</p>
        <%= form_tag budget_items_create_budget_path, method: :post do %>
          <div class="date" data-behaviour="datepicker1">
            <%= label_tag "Month" %>
            <%= text_field_tag 'budget_date', Date.today.at_beginning_of_month, class: "form-control"%><p class="input-group-addon"></p>
          </div>
          <% @categories.each do | cat | %>
            <%= label_tag cat.name %> 
            <%= text_field_tag 'budget_items[][value]', nil, class: 'form-control' %>
            <%= hidden_field_tag 'budget_items[][category_id]', cat.id %>
          <% end %><br/>
          <%= submit_tag("Submit", class: "btn btn-primary") %>
        <% end %>
      </div> 
    </div>
  </div>
  </body>
</html>